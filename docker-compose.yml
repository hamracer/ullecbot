services:
  ullecbot:
    image: ullecbot
    volumes:
      - /mnt/data/ullecbot/db:/opt/data/ullecbot/db
    build:
      context: .
      dockerfile: ./Dockerfile
    labels:
      glance.hide: false
      glance.name: ullecbot
      glance.icon: si:robotframework
      glance.url: https://github.com/hamracer/ullecbot
      glance.description: Dumb Bot
networks: {}
